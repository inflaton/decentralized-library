{
  "name": "d-library-tron",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd dapp-ui && npm start",
    "postinstall": "cd dapp-ui && npm install && npm run build",
    "test:ethereum": "ganache-cli -m 'layer post chase message demise fresh resource beyond bulb rose garden kite'",
    "compile": "tronbox compile",
    "deploy": "tronbox migrate --reset --network mainnet",
    "shasta": "tronbox migrate --reset --network shasta",
    "nile": "tronbox migrate --reset --network nile",
    "dev": "tronbox migrate --reset --network development",
    "test": "truffle test"
  },
  "engines": {
    "node": "12.x"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv-flow": "^3.2.0",
    "ganache-cli": "^6.5.1"
  }
}
