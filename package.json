{
  "name": "ethindia-workshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd dapp-ui && npm start",
    "build": "cd dapp-ui && npm install && npm run build",
    "test:ethereum": "ganache-cli -m 'layer post chase message demise fresh resource beyond bulb rose garden kite'",
    "compile": "tronbox compile",
    "deploy": "source .env && tronbox migrate --reset --network mainnet",
    "shasta": "source .env && tronbox migrate --reset --network shasta",
    "nile": "source .env && tronbox migrate --reset --network nile",
    "dev": "source .env && tronbox migrate --reset --network development",
    "test": "truffle test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "truffle": "^5.0.29"
  },
  "dependencies": {
    "ganache-cli": "^6.5.1",
    "nuxt": "^2.15.7"
  }
}
